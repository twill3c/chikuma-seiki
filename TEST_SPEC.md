# TEST_SPEC.md — chikuma-seiki

<!-- scaffold template v1.12.0 から展開(2026-08-27) -->

## 実行規約

- `pnpm test`(Vitest / `src/**/*.test.ts`)を stage 3–5 の判定に使う。環境は `node`
- stage 5 は `pnpm test` に加えて `pnpm typecheck` と `pnpm build` の 3 点で判定する
- 期待値は SPEC の条項 ID にトレースする。SPEC の保証粒度を超える精度を要求しない(HC-016)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威 | 出典 URL と取得日をフィクスチャ先頭に書く |
| 実測 | 実測日と実測値をコメントに残す |

**このプロジェクトに外部データは無い。** 会社設定(SPEC §2)はすべて創作であり、
期待値の出所は「SPEC §2 の条項」に一本化される。実測由来の定数を置く場合は
その旨をコメントで区別すること。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| 従業員内訳の合計 120 | SPEC §2 | 定義。内訳と総数が食い違ったら落とす |
| 混同行列 / ROC(F-09) | 総当たりしきい値走査 | 独立再計算。実装と別経路で同じ値を出す |
| 段取り替え最小化(F-10) | n ≤ 8 の全順列総当たり | 厳密最適解。発見的解法がこれを下回らないことを検査 |
| 安全在庫(F-11) | 閉形式 z·σ·√L | 解析解。シミュレーション欠品率と許容誤差内で一致 |

## ケース一覧

### 会社設定(F-14)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-14 | 部門別人数の合計 | `HEADCOUNT` の合計が `COMPANY.employees`(120)に一致 |
| T-002 | F-14 | 部門 ID の一意性 | 重複した部門 ID が無い |
| T-003 | F-14 | SPEC §2 の 4 部門が欠けていない | 製造・品質管理・設計生産技術・情報システムが存在し、SPEC §2 の人数と一致 |

### 構造化データ(F-13)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-010 | F-13 | Organization として組み立つ | `@type` が `Organization`、`name` が商号 |
| T-011 | F-13 | 架空である旨が構造化データ側にもある | `disambiguatingDescription` に「架空」を含む |
| T-012 | F-13 / SPEC §6 | 連絡先を出力しない | 直列化に `telephone` / `streetAddress` / `faxNumber` / `email` が現れない |
| T-013 | F-13 | 所在地は都道府県・市までで止まる | `addressRegion` / `addressLocality` はあり、それより細かい鍵は無い |

### 架空明示ゲート(SPEC §6 / N-02)

ソース走査による禁止検査。**うっかり足したら落ちる**状態を維持することが目的。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-020 | N-02 | 写真素材を持たない | `src/` 配下に `.jpg/.jpeg/.png/.webp/.avif/.gif` が存在しない |
| T-021 | SPEC §6 | 電話番号・FAX 番号を書かない | `0d{1,4}-d{1,4}-d{4}` 形の文字列がソースに現れない |
| T-022 | SPEC §6 | 番地を書かない | 「丁目」「番地」「N番M号」がソースに現れない |
| T-023 | SPEC §6 | 認証番号を書かない | 「認証番号」「登録番号」「registration number」がソースに現れない |
| T-024 | SPEC §6 | 資本金・売上高の額を書かない | 「資本金」「売上高」に続く金額表記が現れない |
| T-025 | F-13 | 架空明示の文言が単一の定数から供給される | 明示文の定数が存在し、空でない |

### 図版の決定性(N-03)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-030 | N-03 | 同一入力で同一出力 | ヒーロー図の生成関数を 2 回呼んで完全一致 |
| T-031 | N-03 | `Math.random` を使わない | `src/` 配下に `Math.random` が現れない |
| T-032 | N-03 | 座標が viewBox の内側に収まる | 生成された全点が 0 ≤ x ≤ W, 0 ≤ y ≤ H |

### 工程と設備の対応(T-040〜 / F-04)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-040 | F-04 | 工程 ID の一意性 | `PROCESSES` に重複 ID が無い |
| T-041 | F-04 | 担当部門が実在する | 各工程の `dept` が `HEADCOUNT` の ID に存在する |
| T-042 | F-04 | 設備が実在する | 各工程の `equipment` が `EQUIPMENT` の ID に存在する |
| T-043 | F-04 | 設備の取りこぼしが無い | `EQUIPMENT` の全 ID が、いずれかの工程から参照されている |
| T-044 | F-04 | 管理項目が空でない | 各工程が 1 つ以上の `checks` を持つ |

T-043 は件数ではなく**集合の一致**で書く。設備を足したのにライン図から参照し忘れる、
という取りこぼしを捕まえるための不変量である(HC-016)。

### 受注から出荷までの流れ(T-050〜 / F-03)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-050 | F-03 | 段階 ID の一意性 | `FLOW` に重複 ID が無い |
| T-051 | F-03 | 所要日数が区間として正しい | 各段階で `0 < minDays <= maxDays` |
| T-052 | F-03 | 担当部門が実在する | 各段階の `dept` が `HEADCOUNT` の ID に存在する |
| T-053 | F-03 | 実装工程との接続点が一意 | `containsProcesses: true` の段階がちょうど 1 つ |
| T-054 | F-03 | リードタイムの合計が区間として閉じる | 合計 min ≤ 合計 max、かつ合計 min > 0 |

### SMT ライン図(T-060〜 / F-04・N-03)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-060 | N-03 | レイアウトが決定的 | `lineLayout()` を 2 回呼んで完全一致 |
| T-061 | F-04 | 全工程が図に出る | 駅の数と ID 集合が `PROCESSES` と一致 |
| T-062 | F-04 | 駅が互いに重ならない | どの 2 つの矩形も交差しない |
| T-063 | N-03 | 図の内側に収まる | 全ての駅の矩形が viewBox に含まれる |
| T-064 | F-04 | 搬送路が工程順に各駅を通る | 搬送路の点列に、各駅の中心が工程順の部分列として現れる |

T-064 は「絵として繋がって見えるか」ではなく「順序どおりに通っているか」を検査する。
見た目の検品は `harness/shot.mjs` の目視で行う(loop_001 の VERIF-GAP)。

### AOI × AI 判定シミュレータ(T-070〜 / F-09)

**オラクルの中心は AUC の二経路一致である。** ROC 曲線を台形で積分した AUC と、
曲線を一切経由せず順位対を総当たりして求めた AUC(Mann-Whitney U 統計量)は
恒等的に等しい。実装が曲線の作り方を間違えれば、この二つは合わなくなる。

同じ関数を二度呼ぶ「再計算」は検算にならない。**別の経路で同じ値に着くこと**を要求する。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-070 | N-03 | 基板生成が決定的 | 同じ種で 2 回生成して完全一致 |
| T-071 | F-09 | 欠陥の作り込みが指定どおり | 欠陥を持つ部品数が指定値に一致し、5 種すべてが出現しうる |
| T-072 | F-09 | 部品が基板の内側に収まる・ID が一意 | 全部品の矩形が viewBox 内、ID に重複なし |
| T-073 | F-09 | 混同行列が閉じる | tp+fp+fn+tn が部品数に一致、tp+fn が真の不良数、fp+tn が真の良品数 |
| T-074 | F-09 | しきい値の単調性 | しきい値を上げると tp も fp も単調非増加 |
| T-075 | F-09 | 両端のしきい値 | しきい値 0 以下で全数を不良判定、1 超で全数を良品判定 |
| T-076 | F-09 | **AUC の二経路一致** | 台形積分 AUC と Mann-Whitney U による AUC が 1e-9 以内で一致 |
| T-077 | F-09 | ROC が単調 | fpr・tpr がともに単調非減少で、(0,0) から (1,1) に至る |
| T-078 | F-09 | AI 併用が下回らない | 同じ基板で AI 併用の AUC がルールベースの AUC 以上 |
| T-079 | F-09 | 工数換算が混同行列と整合 | 過検出時間は fp に比例し、fp=0 なら 0 になる |
| T-080 | SPEC §6 | 模擬であることの明示 | デモの説明文に、実測値ではなく模擬である旨が含まれる |

T-078 は「AI の方が賢い」ことを主張するテストではない。**この模型の設計上そうなるように
組んだのだから、そうならなければ模型の実装が壊れている**、という不変量である。

### 段取り替えつき生産計画(T-090〜 / F-10)

**オラクルは全順列の総当たりである。** n ≤ 8 なら順列は高々 40,320 通りなので、
厳密な最適解が求まる。発見的解法(貪欲法・局所探索)がこれを**下回ったら**、
コスト関数が二経路で食い違っている証拠であり、実装が壊れている。

段取り時間は「フィーダを何本載せ替えるか」で決める。実際の多品種少量 SMT の段取りが
それであり、順序依存性が自然に出て、**部品集合の対称差**として厳密に計算できる。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-090 | N-03 | ジョブ生成が決定的 | 同じ種で 2 回生成して完全一致 |
| T-091 | F-10 | 段取り時間の定義 | `setup(a,b)` が固定時間 + 対称差の本数 × 1 本あたり時間に一致 |
| T-092 | F-10 | 段取り時間の性質 | 同一部品構成なら最小、対称であること、非負であること |
| T-093 | F-10 | **原価の二経路一致** | 逐次加算した総時間と、ガントの最終終了時刻が一致 |
| T-094 | F-10 | **発見的解法が最適解を下回らない** | n=7 の全順列総当たり最適値 ≤ 貪欲法・局所探索の値 |
| T-095 | F-10 | 局所探索が貪欲法を悪化させない | 局所探索の値 ≤ 貪欲法の値 |
| T-096 | F-10 | ガントが連続して重ならない | 各区間が前の区間の終了時刻から始まり、順序どおり |
| T-097 | F-10 | 納期遵守数が二経路で一致 | ガントから数えた値と、完了時刻から数えた値が一致 |
| T-098 | F-10 | 重みの両端が意味どおりに効く | 重み 0 の最適解は段取り最小、重み 1 の最適解は遅れ最小(いずれも総当たりと照合) |
| T-099 | F-10 | 順列は全ジョブをちょうど 1 回ずつ含む | どの解法の出力も入力ジョブの並べ替えである |

### ロット番号とトレーサビリティ(T-110〜 / F-05)

**往復検査だけを根拠にしない。** `parse(format(x)) === x` は自己無矛盾を示すだけで、
解釈が正しいことを示さない(furigana-keiryo の HC-035 / fukuo-keiryo の HC-027)。
往復に加えて、(a) 各欄の値域、(b) 不正な入力を棄却すること、の二つを別に置く。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-110 | F-05 | 往復する | `parseLot(formatLot(x))` が x に一致 |
| T-111 | F-05 | 桁の構造が仕様どおり | 形式が `機種3文字-西暦下2桁 + 週2桁-通番3桁` に一致 |
| T-112 | F-05 | **不正な入力を棄却する** | 週 0・週 54・機種 2 文字・通番 4 桁・区切り違いをいずれも `null` で返す |
| T-113 | F-05 | 週の値域 | 1〜53 の全週で往復し、0 と 54 は棄却される |
| T-114 | F-05 | 工程ごとに残す記録が定義されている | 全工程が 1 つ以上の記録項目を持ち、参照する工程 ID が実在する |

### 内訳の扇形(T-120〜 / F-06)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-120 | N-03 | 決定的 | 2 回呼んで完全一致 |
| T-121 | F-06 | 扇の合計が一周する | 中心角の合計が 360 度(誤差 1e-9) |
| T-122 | F-06 | **中心角が人数比と一致する(独立計算)** | 各扇の中心角 ÷ 360 が `count / 総数` に一致 |
| T-123 | F-06 | 扇が隙間なく連続する | 各扇の開始角が前の扇の終了角に一致 |
| T-124 | N-03 | path に NaN が混ざらない | 全 path 文字列に NaN を含まない |

### クリーンルームの気流(T-130〜 / F-05)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-130 | N-03 | 決定的 | 2 回呼んで完全一致 |
| T-131 | F-05 | 流線が天井から床へ下る | 各流線の y が単調非減少 |
| T-132 | N-03 | 図の内側に収まる | 全点が viewBox 内 |

### 沿革(T-140〜 / F-06)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-140 | F-06 | 年が昇順 | 前の項目より小さい年が現れない |
| T-141 | F-06 | 創業年と一致する | 最初の項目の年が `COMPANY.founded` |
| T-142 | F-06 | 未来の出来事を書かない | 全ての年が 2026 以下 |

### OG 画像・採用・問い合わせ(T-150〜 / F-07・F-08・F-15)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-150 | F-15 | OG の文言が全対象ページに定義されている | `OG_CONTENT` の鍵集合が対象ルートの集合と一致 |
| T-151 | F-15 | OG の寸法が規定どおり | 1200 × 630 |
| T-152 | F-15 | OG に架空である旨が乗る | どの文言にも「架空」を含む脚注がある |
| T-153 | N-07 | **実行時の外部通信を持たない** | `fetch` / `XMLHttpRequest` / `WebSocket` が現れない(ビルド時の `og-font.ts` を除く) |
| T-160 | F-07 | 求人が実在の部門を参照する | 各求人の `dept` が `HEADCOUNT` に存在 |
| T-161 | F-07 | 全部門が採用ページで説明される | `HEADCOUNT` の全 ID がいずれかの求人から参照される |
| T-162 | F-08 | 送信できないフォームである | 項目定義に送信先を持たず、`<form>` に `action` が無い |
| T-163 | F-08 | 問い合わせページに架空明示がある | `COMPANY.contactNotice` が空でなく「受け付けていません」を含む |

### 部品の欠品リスクと安全在庫(T-170〜 / F-11)

**オラクルは「サイクル欠品率 ≈ 1 − Φ(z)」の恒等式である。** 発注点を
`μ·L + z·σ·√L` に置くと、リードタイム中に在庫が尽きる確率は理論上 `1 − Φ(z)` になる。
閉形式で出した値と、シミュレーションで数えた実測が許容誤差内で一致することを要求する。

乱数は種から回すので、この検査は決定的でありフレークしない。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-170 | F-11 | 正規分布の分位点と累積分布が逆関数の関係にある | `normalCdf(normalQuantile(p))` が p に一致(1e-6) |
| T-171 | F-11 | **外部権威との照合** | z(0.95)=1.6449、z(0.99)=2.3263、z(0.975)=1.9600(標準正規分布表・1e-3) |
| T-172 | F-11 | 安全在庫が √L に比例する | リードタイムを 4 倍にすると安全在庫が 2 倍になる |
| T-173 | F-11 | 安全在庫がサービス水準について単調増加 | 水準を上げると安全在庫が減らない |
| T-174 | N-03 | シミュレーションが決定的 | 同じ種で 2 回回して完全一致 |
| T-175 | F-11 | **実測の欠品率が閉形式と一致する** | 十分な回数で `\|実測 − (1 − Φ(z))\|` が許容誤差内 |
| T-176 | F-11 | 在庫推移が破綻しない | 発注は 1 サイクルに 1 回、入荷はリードタイム後 |
| T-177 | F-11 | 在庫金額が安全在庫と単価から再計算できる | 別経路の計算と一致 |

### フッタとナビ(T-190 / F-16・N-06)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-190 | N-06 | フッタが規約どおり 5 項目 | 項目数と並びがフリート共通規約に一致 |
| T-190 | F-16 | 公開待ちが残っていない | `href` が null の項目が 0 件 |
| T-190 | F-16 | リンク先が https の絶対 URL | 全項目が `https://` で始まる |
