<!-- scaffold:block agents_core v1.12.0 -->
## 共通規律(scaffold 管理領域 — 手動編集禁止)

このセクションはスキャフォールド・レジストリが管理する。内容を変更したい場合は、
このファイルを直接編集せず、失敗ログ → HARNESS_CHANGELOG 起票 → レジストリ改訂 → `scaffoldctl update` の経路で行うこと。

### 7 段階ループプロトコル

| 段階 | 名称 | 完了条件 |
|---|---|---|
| 1 | 計画 | 対象の要求 ID を特定し、`loop_start` を記録した |
| 2 | 文脈読込 | SPEC.md / IMPLEMENTATION_GUIDE.md の該当箇所と、直近ループのログを読んだ。**この段階で扱うデータ・設定ファイルの実物を開いた**(索引・要約・記憶ではなく、本文・設定そのもの — HC-014) |
| 3 | テスト先行 | TEST_SPEC.md にトレースする失敗するテストを書き、赤を確認した。**各ケースの期待値の出所(SPEC の条項 / 実測 / 外部権威)を書いた**(HC-016) |
| 4 | 実装 | ファイル編集 2 回ごとにテストを実行し、赤のまま次の編集に進んでいない |
| 5 | 検証 | 全テスト合格 + 独立再計算(該当時)を確認した |
| 6 | 文書同期 | SPEC/docs と実装の乖離(SPEC-DRIFT)を解消し、生成ドキュメントを再生成した |
| 7 | 完了 | `stage_end` → `loop_end` の順に記録し(**`loop_end` はループの最終行**。後から追記しても LL-09 で拒否される — HC-013)、ループログ validate に合格し、専用コミットを積んだ |

### ループ可観測性

全ループは loop-observability の規律(LOOP_LOG_SPEC / FAILURE_TAXONOMY)に従い
`logs/loops/{loop_id}.jsonl` に記録する。失敗は気づいた瞬間に分類コード付きで記録する。
ツーストライク(LL-10)と S1 即時起票(LL-12)は本プロジェクトでも有効である。

**`looplog.py append` の出力を破棄してはならない**(`>/dev/null` 等)。記録は拒否されることがあり、
出力を捨てると「記録できていないこと」に気づけない。毎回、成否を目視で確認する(HC-013)。

### 外部データの採録(HC-012)

外部のコーパス・データセットから採録対象を選ぶとき:

- **メタデータからの推定で確定しない。** 題名・分類番号・ラベルは候補生成にのみ使い、
  本文・実物を見るまで `needs_review` を外さない
- **抽出器を書く前に件数オラクルを探す。** コーパス自身が持つ手がかり(題名の数詞・目次・凡例の記載・
  公表された総数)を先に探し、見つからなければ「オラクル無し」と SPEC に明記する

### 期待値の書き方(HC-016)

テストの期待値を**観測する前に定数で書いてはならない**。書いた定数を仕様だと思い込むと、
正しい実装・正しいデータの方が落ちる(VERIF-FALSE)。

- **SPEC の保証粒度を超えない。** 超える精度を要求したくなったら、先に SPEC 側を上げる。
  例: SPEC が「暦日の一致」しか保証していないのに「分の一致」を要求してはならない
- **件数・行数は定数で書かない。** 「集合が一致する」「取りこぼしが無い」という不変量で書く。
  外部データは重複・欠落を含むので、数はデータが動くたびに壊れる
- **やむを得ず定数で書くなら、実測日と実測値をコメントに残す。** 後から見て
  「誰かの仮定」か「その日の観測」かが区別できない期待値を残さない

### エスカレーション規範

以下の場合は作業を止め、`escalation` を記録してから人間に確認する:
仕様の複数解釈(SPEC-AMB 相当)/ スコープ外ファイルへの変更が必要になった /
破壊的操作(履歴改変・データ削除・強制 push)/ 同種の修正の 3 回目の失敗(PROC-LOOP)。

### コミット規約

Conventional Commits(feat/fix/test/docs/refactor/chore)。スキャフォールド更新は
`chore: scaffold vX.Y.Z` の専用コミットで行い、機能変更と混ぜない。
<!-- /scaffold:block agents_core -->

<!-- ここから下はプロジェクト固有。scaffold:block の中は触らないこと -->

## chikuma-seiki 固有の規律

### 何を作っているか

長野県佐久市の**架空の EMS 企業**「株式会社 千曲精機」のコーポレートサイト。
ポートフォリオ作品であり、実在企業のサイトではない。詳細は `SPEC.md`。

主役は `/technology` の 3 つのデモ(AOI × AI 判定 / 生産計画最適化 / 部品欠品リスク)であり、
他のページはその文脈を与えるために存在する。**語る代わりに実演する**、が設計の背骨。

### 実行

```bash
pnpm test          # Vitest(src/**/*.test.ts)
pnpm typecheck     # tsc --noEmit
pnpm build         # next build → out/ に静的書き出し
MSYS_NO_PATHCONV=1 node harness/shot.mjs           # トップを撮る
MSYS_NO_PATHCONV=1 node harness/shot.mjs / /technology   # 複数ページ
```

`harness/shot.mjs` は `out/` を実ブラウザで開いて撮る。**ビルドが通ったことと絵が読めることは別**
なので、図を触ったループでは必ず撮って目視する(loop_001 の VERIF-GAP)。

### 架空明示ゲートは緩めない(SPEC §6)

`src/lib/guard.test.ts` が、出荷されるソースから禁止表現を締め出している。
番地・電話番号・認証番号・取引先実名・資本金の額・`Math.random`。**落ちたらまず表現を直す。**
ゲートの側を緩めるのは、誤検出だと確認できたときだけ。

ゲートはコメントを走査対象から外している(`src/lib/strip-comments.ts`)。
コメントはバンドル時に落ちて読者に届かないためであり、
「番地を持たない」と方針を書いたコメント自体が落ちる事故を防ぐ(loop_001 の VERIF-FALSE)。

**走査対象が 0 件のとき、ゲートは空振りで合格する。** そのため
`guard.test.ts` の先頭に「走査すべきファイルが実際に見つかっている」ケースを置いてある。
消してはならない。

### 数字は一箇所から出す(F-14)

従業員数・部門別人数・創業年は `src/data/company.ts` だけが持つ。
ページに直接書かない。書くと必ずどこかで食い違い、架空企業の設定としての一貫性が崩れる。

### 図は式で描く(N-02 / N-03)

写真素材を一枚も持たない。`Math.random` を使わず、種を渡す線形合同法で決める。
乱数を使うと静的書き出しと再描画で絵が変わり、ハイドレーション不整合になる。

配線は**直交と 45 度だけで曲がる**(`src/lib/hero.ts` の `route`)。
基板の配線は鋭角に曲げないという実際の約束をそのまま形にしており、テストで固定してある。

### 暗い図を暗い背景に置くとき

`sugi-nami` と同じ落とし穴を踏んだ。不透明度 0.1〜0.3 の線は背景に沈んで**絵に見えない**。
loop_001 では等高線を 0.26+level×0.055 まで上げ、線だけでなく**ごく薄い塗り**を重ねて
初めて地形として読めるようになった。文字を乗せる側は、図全体を暗くするのではなく
**読ませたい範囲にだけ幕を掛ける**(全体を暗くすると配線が沈む)。

### この機の癖

- **Bash 呼び出しをまたいで cwd が持ち越される。** `looplog.py` は毎回
  `python harness/looplog.py ...` の形で叩く(HC-038)
- **Git Bash は `/` で始まる引数を Windows パスに変換する。** `harness/shot.mjs` に
  ルートパスを渡すときは `MSYS_NO_PATHCONV=1` を前置する(HC-038)
- Python の `str.replace` は不一致でも例外にならない。ソース修正は `sed` で行い、
  置換後の行を `sed -n` で目視する(loop_001 の TOOL-MISUSE)

### 図に添える文字は、図を生成したデータから導く(HC-039)

ライン図の「投入」「次工程へ」を viewBox の左下・右下に決め打ちしたところ、
蛇行で下段が右から左へ流れるため出口が左下にあり、**図は正しいのにラベルだけが矛盾**した。
修正でラベルを搬送路の端点に置き直したら、今度は駅の矩形に重なった。

- 生成関数は骨格だけでなく、**ラベルが必要とする点**も返す(`lineLayout()` の `entry` / `exit`)
- ラベルの位置がデータから導かれていることをテストで固定する(T-065)
- 図の側の不変量(順序・非重なり・境界)が緑でも、ラベルの矛盾は捕まらない

### データの相互参照は ID で結び、集合で検査する

`PROCESSES` の `dept` は `HEADCOUNT` を、`equipment` は `EQUIPMENT` を ID で指す。
実在しない ID を書いたら落ちる(T-041 / T-042)。さらに **T-043 は逆向き** — 
`EQUIPMENT` の全設備がいずれかの工程から参照されていることを集合で検査する。
設備を足してライン図から参照し忘れる、という取りこぼしはこちらでしか捕まらない。

### デモの正しさは「二経路一致」で担保する

同じ関数をもう一度呼んで比べても検算にならない。**別の経路で同じ値に着くこと**を要求する。

- F-09: ROC を台形で積分した AUC ==(1e-9 以内)== 順位対を総当たりした AUC(Mann-Whitney U)
- F-10: 局所探索の解 <= n<=8 の全順列総当たりの最適解
- F-11: シミュレーションの欠品率 ==(許容誤差内)== 閉形式 z·σ·√L

デモを足すときは、動くものより先にこの一行を決める。決められないものは載せない。

### 模擬データの率を、実機の規模に掛けない(loop_003)

デモの基板は見やすくするために欠陥を多く仕込んである(6/48 = 実機より二桁高い)。
その過検出率を実機相当の検査点数(1,200 点/枚)に掛けたところ、
**目視再検査 8,993 時間/月**という桁の壊れた数字が出た。

換算は**模擬ラインの中で閉じる**。検査点数もこの模擬基板そのもの(48 点/枚)を使い、
「この模擬ラインを月 1,800 枚流したら」という条件つきの数字として出す。
画面にも「絶対値ではなく増減を見てほしい」と書く。

テストには**桁の上限**を置いてある(全点を過検出した極端値を超えないこと)。
率の整合だけを見るテストは、この種の桁違いを素通りさせる。

### デモの模型を強くしすぎない(loop_003)

AI 併用の AUC が 1.000 に張り付き、ROC が完全な直角になった。
「AI は万能」という嘘の絵になるので、パラメータを弱めて
rule 中央値 0.837 / ai 中央値 0.956 / 完全分離 2 種(50 種中)に調整した。
`aoi.test.ts` に「50 種中 5 種を超えて完全分離しないこと」を置いてある。

### SVG の `<title>` は単一のテキスト子要素にする(HC-037)

複数の JSX 式を並べると React が配列として扱い、
**ハイドレーション不整合(#418)**になる。文字列を返す関数にまとめること。
HC-037 が senoto-mori で記録済みの型だが、当該 HC は未展開だったので同じ型を踏んだ。

### デモの「綱引き」が実在することをテストで固定する(loop_004)

生産計画のデモで、納期を総所要時間と無関係な一様分布で振ったところ、
**全ジョブが余裕で間に合い、遅れが常に 0** になった。
「段取りを詰める vs 納期を守る」のつまみが何も動かさないデモになる。

計算は正しく、オラクル(全順列総当たり)とも一致していた。**壊れていたのは問題設定の方**である。
T-100 に三つ置いてある:

- 受注順のままなら必ずどれかが納期に落ちる
- 重み 0 と重み 1 で選ばれる投入順が変わる(20 種中 16 種以上)
- 段取り最小の解は納期優先の解より遅れが大きい(同上)

**デモを足すときは「操作しても何も変わらない」状態をテストで禁止する。**
正しく計算しているだけのデモは、動いていないのと同じ。

### 実測値は毎回出力する(loop_004)

`局所探索が最適解に一致した割合: 34/60(57%)` をテストが毎回 console に出す。
閾値だけを見ていると「ぎりぎり通っている」のか「余裕で通っている」のかが分からない。
数字を出しておけば、パラメータを触ったときの影響がその場で見える。

### 禁止・検出系のテストには陽性対照を置く(HC-041)

`src/lib/guard.test.ts` の `RULES` は、パターンごとに
`catches`(必ず捕まえるべき悪い例)と `allows`(撃ってはならない正当な例)を
対で持っている。**この対照が無い検査は、緩めたときも壊れたときも緑のままになる。**

実際、対照を足した初回に電話番号パターンの穴が出た。
`0\d{1,4}-\d{1,4}-\d{4}` は末尾を 4 桁に固定していたため、
フリーダイヤル `0120-000-000` を取りこぼしていた。
5 ループのあいだ「違反 0 件」で緑だったが、検査できていなかっただけである。

パターンを絞るときは `allows` を、緩めるときは `catches` を必ず増やす。

### 往復検査を正しさの根拠にしない(HC-027 / HC-035)

`parse(format(x)) === x` は自己無矛盾を示すだけで、解釈が正しいことを示さない。
ロット番号では往復(T-110)に加えて、桁の構造(T-111)と
**不正な入力を 10 種棄却すること**(T-112 / T-113)を別の根拠として置いてある。
往復検査は「正しい入力」しか通さないので、間違った番号も往復してしまう。

### 地図・略図のラベルには下敷きを敷く(loop_005)

略図では線とラベルの交差を避けきれない(河川と道路は実際に交わる)。
位置をずらして逃げるとラベルが指すものから離れるので、
`AreaMap` の `MapLabel` のように背後を塗る。

なお略図は**縮尺も座標も持たない**。実在の区画を指さないための設計である(SPEC §6)。

### エスケープを含む編集を多段の経路で行わない(HC-042)

「bash ヒアドキュメント → Python 文字列 → ファイル」で正規表現を書き込んだところ、
改行のエスケープが実際の改行になり、二重の逆斜線が一本に潰れ、
**単語境界のエスケープが制御文字 U+0008 になった**。

(この節自体、最初は同じ経路で書いたために壊れた。エスケープ記号を本文に置くと
また潰れるので、ここでは記号を使わず言葉で書いてある。)

最後のものが特に悪い。ソースを開いても grep しても**見た目に何も無い**うえ、
構文エラーにもならないので typecheck も通る。パターンは
「バックスペースに続く fetch」しか見ない状態になり、**検査が無言で死ぬ**。

気づけたのは陽性対照(T-153)が落ちたからで、それが無ければ緑のまま残っていた。
エスケープ・正規表現・改行を含む編集は、**ファイル編集ツールで直接書く**。

### 文字種の検査を置く(HC-037)

`src/lib/text-hygiene.test.ts` が、キリル・ハングル・タイ文字の混入を落とす。
loop_006 で実際に「измерение」が採用ページの本文に混入した。<!-- text-hygiene:allow -->
字形が似ているので目視では見つからず、型検査も build も通る。

**実例を文書に残すと、その行が検査に引っかかる。** 上の行には行単位の除外マーカー
`text-hygiene:allow` を付けてある。実例を消すと教訓が痩せるので、消さずに除外する。
ただしマーカーが黙って増えると検査が骨抜きになるので、
**混入していない行にマーカーが付いていたら落ちる**ようにしてある。

### OG 画像の見出しは行に割ってから渡す(loop_006)

Satori に折り返させると語の途中で割れる(「少しず／つ」になった)。
`OG_CONTENT.title` は `string[]` で、1 行の上限字数をテストで固定してある。
描く字は文言から機械的に集めて `text=` に渡す(取り漏らすと豆腐になる)。

### オラクルが循環していないか疑う(loop_007)

在庫のデモで、シミュレーションが閉形式と**同じ集約分布** N(μL, σ√L) から
一度に需要を引いていた。欠品判定が「Z > z」に還元され、
1 − Φ(z) との一致がほぼ恒等式になる。テストは緑だが、何も検査していない。

このデモの主張は「安全在庫はリードタイムの平方根に比例する」であり、
その根拠が σ√L という集約則である。**検査したい当のものを前提に使ってはならない。**
週ごとに独立に引いて足す形に変え、集約則を実装から導かせている。

**新しいオラクルを書いたら、「これは主張したいことを前提にしていないか」を一度疑う。**

### 画面に出す実測値はログと突き合わせる(F-12)

`/technology` の「このサイト自体の記録」は、架空の会社の実績ではなく
このリポジトリの `logs/loops/*.jsonl` の実測である。
`src/data/practice.test.ts` がログを数え直して照合するので、
**ループを回すたびに数字がずれてテストが落ちる**。落ちたら数字を更新する
(`node bin/practice-stats.mjs` で数えられる)。架空の実績と実在の記録を混ぜないための仕掛け。

### 検品の道具が壊れていないか(loop_007)

OG 画像のルートを足したら `out/technology` がディレクトリになり、
静的サーバが `/technology` を 404 にした。**404 のページを撮っても
「撮影しました」と出る**ので、しばらく気づかなかった。
`harness/shot.mjs` は 404 を検出したら終了コードを 1 にする。
撮影の出力に「404 になった要求」が出たら、その画像は信用しない。
