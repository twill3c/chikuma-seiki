/**
 * /technology に載せるデモの説明(F-09 / F-10 / F-11)。
 *
 * どのデモも**模擬データによる実演**であり、実測値ではない。
 * 架空の会社が実績値を出しているように読まれると SPEC §6 の趣旨に反するので、
 * その明示は各デモの定義に必ず持たせる(T-080)。
 */
export type Demo = {
  id: string;
  name: string;
  lead: string;
  /** 何が難しいのか。デモの前に置く「問い」 */
  problem: string;
  /** 模擬であることの明示。空にできない */
  simulationNotice: string;
  /** この計算の正しさを何で担保しているか */
  oracle: string;
  ready: boolean;
};

export const DEMOS: readonly Demo[] = [
  {
    id: "aoi",
    name: "自動光学検査と AI",
    lead: "見逃しと過検出は、片方を減らすともう片方が増える。",
    problem:
      "AOI のしきい値を厳しくすれば見逃しは減りますが、良品まで弾いて目視再検査が積み上がります。多品種少量では機種ごとに癖が違うので、この見極めが毎回必要になります。AI(画像認識)の効き目は、欠陥を余計に見つけることではなく、良品なのに上がってしまう見かけの信号を割り引けることにあります。",
    simulationNotice:
      "この画面は模擬データによる実演です。基板・欠陥・スコアはすべて計算で作ったもので、実際の検査結果ではありません。工数の換算に使った前提値は画面下部に明示しています。",
    oracle:
      "ROC 曲線を台形で積分した AUC と、曲線を経由せず順位対を総当たりして求めた AUC(Mann-Whitney U)が厳密に一致することを、50 種類の基板で検査しています。",
    ready: true,
  },
  {
    id: "schedule",
    name: "段取り替えと生産計画",
    lead: "動いている時間より、切り替えている時間が長い日がある。",
    problem:
      "多品種少量では、投入する順番を変えるだけで一日に作れる枚数が変わります。似た機種を続けて流せば段取り替えは短くなりますが、納期の遠い機種を先に流すことになりがちです。",
    simulationNotice:
      "この画面は模擬データによる実演です。ジョブ・段取り替え時間・納期はすべて計算で作ったもので、実際の生産実績ではありません。",
    oracle:
      "小規模な問題では全順列を総当たりして厳密な最適解を求め、発見的解法がそれを下回らないことを検査しています。",
    ready: false,
  },
  {
    id: "inventory",
    name: "部品の欠品リスクと安全在庫",
    lead: "一点でも欠ければ、ラインは組めない。",
    problem:
      "半導体の一部は納期が数か月単位で動きます。在庫を厚くすれば欠品は減りますが、多品種少量では機種ごとに専用部品を抱えることになり、そのまま資金が寝ます。",
    simulationNotice:
      "この画面は模擬データによる実演です。需要・納期・在庫はすべて計算で作ったもので、実際の調達実績ではありません。",
    oracle:
      "正規需要に対する安全在庫の閉形式と、シミュレーションで測った欠品率が許容誤差内で一致することを検査しています。",
    ready: false,
  },
] as const;

export function readyDemos(): Demo[] {
  return DEMOS.filter((d) => d.ready);
}
