# SPEC.md — chikuma-seiki(株式会社 千曲精機 コーポレートサイト)

<!-- scaffold template v1.12.0 から展開(2026-08-27)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

長野県佐久市に所在する架空の EMS(電子機器受託製造)企業「**株式会社 千曲精機**」の
コーポレートサイトを作る。ポートフォリオ作品であり、実在企業のサイトではない。

主眼は「会社案内が綺麗に作れること」ではなく、**この会社が事業として掲げている技術
(AI による外観検査、生産計画の最適化、データ分析)が、サイト上で実際に動いて見えること**にある。
語る代わりに実演する。したがって `/technology` の 3 つのデモが本作品の主役であり、
他のページはその文脈を与えるために存在する。

## 2. 会社設定(架空・全ページで一貫させること)

| 項目 | 値 |
|---|---|
| 商号 | 株式会社 千曲精機(ちくませいき / Chikuma Seiki Co., Ltd.) |
| 所在地 | 長野県佐久市(佐久インターチェンジ付近の工業団地内)**番地は持たない** |
| 従業員数 | 約 120 名 |
| 内訳 | 製造現場 78 / 品質管理 10 / 設計・生産技術 10 / 情報システム 7 / 管理・営業 15 |
| 事業 | ① EMS 受託製造(産業用ロボット向け制御基板、医療機器向けセンサモジュール) ② 自動化システム・データ分析システムの受託開発 |
| 生産方式 | 多品種少量。高信頼性が要求されるニッチな高付加価値品に強み |
| 主要設備 | 表面実装機(マウンタ)、自動光学検査装置(AOI)、クリーンルーム(クラス 10000 対応) |

内訳の合計は 120 に一致すること(F-14 でテストする)。

## 3. 機能要求

### 3.1 ページ

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-01 | `/` トップ。事業 2 本柱・数字・ライン図の抜粋・SVG ヒーロー | must | 1 |
| F-02 | 全ページ共通のヘッダ/ナビ/フッタ。フリート共通フッタ規約 + 架空明示 | must | 1 |
| F-03 | `/business` 事業内容。EMS / 受託開発の 2 本柱と受注〜出荷フロー | must | 2 |
| F-04 | `/facility` 設備・工程。クリック可能な SMT ライン図(印刷→マウント→リフロー→AOI→ICT→組立) | must | 2 |
| F-05 | `/quality` 品質保証。検査体制・トレーサビリティ・クリーンルーム気流断面図 | must | 5 |
| F-06 | `/company` 会社案内。沿革・120 名の内訳図・所在地略図 | must | 5 |
| F-07 | `/recruit` 採用。職種別(製造 78 / 品管 10 / 設計 10 / 情シス 7) | should | 6 |
| F-08 | `/contact` 問い合わせ。**送信できない**フォームと架空である旨 | must | 6 |

### 3.2 デモ(本作品の主役)

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-09 | `/technology` デモ①: **AOI × AI 判定シミュレータ**。SVG 生成基板に 5 種の欠陥を仕込み、判定しきい値スライダで見逃し/過検出のトレードオフを提示。ルールベース単独と AI 併用を切替えて ROC を比較し、過検出削減による目視再検査工数の削減時間を算出する | must | 3 |
| F-10 | `/technology` デモ②: **段取り替えつき生産計画最適化**。20 前後のジョブを順序付け、ガントチャートで納期遵守率と稼働率を提示。FIFO → 貪欲法 → 局所探索で総段取り時間が縮む過程を見せる | must | 4 |
| F-11 | `/technology` デモ③: **部品欠品リスクと安全在庫**。長納期部品の需要変動に対し、発注点(s)と安全在庫から欠品率・在庫金額のトレードオフを提示する | must | 7 |
| F-12 | `/technology` に「AI エージェントを活用した開発の進め方」の節(テスト先行・オラクル照合・失敗の記録) | should | 7 |

### 3.3 横断

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-13 | 架空である旨を **4 箇所**(フッタ / `/company` / `/contact` / JSON-LD)に明示する | must | 1 |
| F-14 | 会社設定(§2)を単一のデータ源から供給し、全ページが同じ数字を出す | must | 1 |
| F-15 | OG 画像(`next/og`)。トップと `/technology` に個別の絵 | should | 6 |
| F-16 | フッタ 5 項目のうち GitHub / 歩き方 / 設計図 / MIT の 4 リンクを実体に接続する。公開待ちの間はリンクにせず文字だけ出す(`src/data/site.ts` の `FLEET_FOOTER`) | must | 7 |

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Next.js 15 App Router + `output: "export"` の**完全静的**。サーバ実行時計算を持たない | `next build` が `out/` を生成し、`.next/server` に動的ルートが無いこと |
| N-02 | **写真素材を一切使わない。** 図版はすべて SVG で構成する | `src/` 配下に `.jpg/.jpeg/.png/.webp/.avif` が存在しないこと(テスト) |
| N-03 | SVG は乱数を使わず式で決める。再描画・静的書き出しで同じ絵になる | 同一入力に対する生成関数の出力一致(テスト) |
| N-04 | デモの計算ロジックは純関数として `src/lib/` に切り出し、DOM に依存しない | Vitest で単体テスト可能であること |
| N-05 | 配色は鉄紺 × 白 × 銅の一枚。明暗切替を持たない | `@theme` のトークン定義に一本化 |
| N-06 | フッタはフリート共通 5 項目(MIT / GitHub / 歩き方 / 設計図 / App Menu)+ 架空明示 | DOM テスト |
| N-07 | Vercel へ静的デプロイ。ランニング費用を発生させる実行時要素を持たない | **実行時**の外部通信(`fetch` / `XMLHttpRequest` / `WebSocket`)がソースに現れないこと。**ビルド時**の取得は例外とし、対象を `src/lib/og-font.ts` に限る(OG 画像の日本語フォント) |

## 5. 品質基準

- `pnpm typecheck` / `pnpm build` / `pnpm test` の 3 つが緑であること(各ループの stage 5 条件)
- デモの計算ロジックには**独立したオラクル**を置く:
  - F-09: 混同行列は総当たりのしきい値走査で厳密に再計算できること。さらに **ROC の台形積分による
    AUC が、順位対の総当たり(Mann-Whitney U)で求めた AUC と厳密に一致すること** — 曲線を経由する
    経路と、曲線を経由しない経路の二つで同じ値に着くことを要求する
  - F-10: 小規模インスタンス(n ≤ 8)で**全順列の総当たり最適解**と局所探索解を比較し、局所探索が最適解を超えないこと
  - F-11: 正規需要に対する安全在庫の閉形式(z·σ·√L)とシミュレーション欠品率が許容誤差内で一致すること
- 架空明示は 4 箇所すべてが DOM に出ていること。**禁止語テスト**(下記)が緑であること

## 6. 架空明示ゲート(この作品の安全装置)

製造業のサイトは飲食店より実在誤認の害が大きい(取引・与信・採用に使われうる)。
以下をテストで**禁止**する。うっかり足したらテストが落ちる状態を維持すること。

| 禁止対象 | 理由 |
|---|---|
| 番地(丁目・番地・号を含む住所) | 実在の区画を指さない |
| 電話番号 / FAX 番号 | 実在企業への誤接続を防ぐ |
| ISO 9001 / ISO 13485 等の**認証番号** | 認証詐称になりうる(制度の説明として名称に触れるのは可) |
| 取引先の実名 | 実在企業の名誉・信用に触れる |
| 人物写真・氏名入りの社員紹介 | 実在人物の誤認 |
| 具体的な売上高・資本金の額 | 与信情報として誤用されうる |
| JSON-LD の `telephone` / `streetAddress` / `faxNumber` | 構造化データとして機械に拾われる |

JSON-LD には `disambiguatingDescription` に架空である旨を必ず入れる。

## 7. スコープ外

- 多言語化(英語版)。`sugi-nami` と違い当面は日本語のみ
- 実在の認証・規格への適合主張、実在企業・製品名の掲載
- 問い合わせフォームの送信機能(F-08 は意図的に送信できない)
- CMS・記事投稿基盤。お知らせはソース内のデータで持つ
- 明暗テーマ切替(N-05)
